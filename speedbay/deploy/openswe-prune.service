[Unit]
Description=Prune stale Open SWE Docker resources
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
User=openswe
ExecStart=/usr/bin/docker container prune -f --filter label=openswe.sandbox=1 --filter until=168h
StandardOutput=journal
